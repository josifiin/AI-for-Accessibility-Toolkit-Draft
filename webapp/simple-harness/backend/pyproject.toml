[project]
name = "simple-harness-backend"
version = "0.1.0"
dependencies = [
    "fastapi",
    "uvicorn",
    "websockets",
    "google-genai",
    "python-dotenv",
]

[tool.uv]
dev-dependencies = []
